// Classe de base pour tous les acteurs du système
// Ici, tu pourras ajouter des méthodes ou propriétés communes à tous les types d'acteurs
export class GuildesActor extends Actor {
  // Préparation des données dérivées (calculs automatiques)
  prepareDerivedData() {
    super.prepareDerivedData();
    // Ajoute ici tes calculs personnalisés si besoin
  }
}